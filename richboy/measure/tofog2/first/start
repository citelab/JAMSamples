#djam run compute.jxe --cloud --app=first --bg

for i in `seq 1 4`; do
    djam exec --type=fog --app=second --program=compute --xapp=first compute.jxe --bg --log
done

for i in `seq 1 16`; do
    djam exec --app=second --program=compute --xapp=first compute.jxe --tags=$i --bg
done
