djam init --zones=1 --indelay=3:1 --outdelay=5:3 --cldelay=30:5

#djam run overhead.jxe --cloud --app=overhead --bg

#for i in `seq 1 3`; do
    djam run overhead.jxe --fog --app=overhead --bg
#done

for i in `seq 1 5`; do
    djam run overhead.jxe --app=overhead --bg
done
