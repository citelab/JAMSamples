#include <stdlib.h>

int q;

int main() {

    while (1) {
            temp = rand() % 200 + 0.1 * (rand() % 10);
            pos = rand() % 10;

	    // printf("Temp %f , pos %f\n", temp, pos);
	    //      q = slid;
	    //            printf("Slider value %d\n", q);
    }
}
